/* import { ListVideos } from "@components/videos/TrendingVideos"; */
import Video from "@/components/videos/Video";

export const SuggestionVideos = () => {
    return (
        <div className="rounded-xl border-border border flex flex-col mt-6 px-3 py-2">
            {/*  <ListVideos>
                {Array(4)
                    .fill(0)
                    .map((_, index) => (
                        <Video key={index} />
                    ))}
            </ListVideos> */}
        </div>
    );
};
